export interface ThemeColors {
  background: string;
  mainText: string;
  subText: string;
  cardBackground: string;
  border: string;
  errorBackground: string;
  errorBorder: string;
  errorText: string;
  retryButtonBackground: string;
  placeholderText: string;
  todoInputBackground: string;
  buttonBackground: string;
  white: string;
  colorCompleted: string;
  green: string;
}